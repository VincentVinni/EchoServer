# Flask and extensions
Flask                # Web framework for building the API
Flask-Cors          # Optional: Enable Cross-Origin Resource Sharing (CORS) if needed

# Supabase
supabase             # Supabase Python client

# Environment Variable Management
python-dotenv         # Load .env files for environment variables

# Data Validation (Optional, for schema validation)
marshmallow         # For defining and validating schemas

# Additional Utilities
requests          # Optional: For handling HTTP requests (if needed)
